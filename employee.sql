Create free mysql database and then create employee table having id, firstName, firstName as fields

CREATE database mysql;
USE mysql;
create table employee (
  id INT UNSIGNED AUTO_INCREMENT,
  firstName VARCHAR(30),
  firstName VARCHAR(30),
  PRIMARY KEY(id));
 
